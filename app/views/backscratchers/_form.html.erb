<%= form_with(model: backscratcher, local: true) do |form| %>
  <% if backscratcher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(backscratcher.errors.count, "error") %> prohibited this backscratcher from being saved:</h2>

      <ul>
      <% backscratcher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_area :price %>
  </div>

  <div class="field">
    <%= form.label :sizes %>
    <%= form.text_area :sizes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
